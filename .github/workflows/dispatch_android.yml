# .github/workflows/dispatch_android.yml

name: Build and Release Android App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Optional tag to run (e.g. v2.7.10). Leave empty to run branch.'
        required: false
        default: ''

jobs:
  build_and_release:
    name: Build ${{ matrix.package_suffix }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: android7
            package_suffix: Android7
          - variant: android11
            package_suffix: Android11

    steps:
      # 步骤 1: 检出代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 如果在手动触发时填写了 tag，则切换到该 tag
      - name: Checkout specified tag
        if: ${{ github.event.inputs.tag != '' }}
        run: |
          git fetch --tags
          git checkout tags/${{ github.event.inputs.tag }}

      # 新增步骤: 配置 SSH 以访问私有依赖库
      - name: Configure SSH for Private Dependencies
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      # 步骤 2: 设置 Java 环境
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # 步骤 3: 缓存 Flutter 依赖
      - name: Cache Flutter dependencies
        uses: actions/cache@v3
        with:
          path: /opt/hostedtoolcache/flutter
          key: ${{ runner.os }}-flutter-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-flutter-

      # 步骤 4: 设置 Flutter 环境
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.1'
          channel: 'stable'

      # 步骤 5: 获取 Flutter 依赖包 (现在可以成功访问私有库)
      - name: Get Flutter dependencies
        run: flutter pub get

      # 步骤 6: 解码并配置签名文件
      - name: Decode Keystore
        run: |
          echo "${{ secrets.KEYSTORE_JKS }}" | base64 --decode > android/app/upload-keystore.jks
          # 创建 Gradle 打包所需的 keystore.properties 文件
          echo "storeFile=upload-keystore.jks" > android/keystore.properties
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" >> android/keystore.properties
          echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> android/keystore.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/keystore.properties

      # 步骤 7: 使用对应 YAML 和锁文件构建 Android 7 / Android 11 APK
      - name: Build Flutter APK (${{ matrix.package_suffix }})
        env:
          VARIANT_USE_SYSTEM_FLUTTER: 'true'
        run: |
          dart run tool/variant.dart build-apk "${{ matrix.variant }}" --env prod --release
      
      # 步骤 7.1: 生成版本信息文件
      - id: generate_version_info
        name: Generate Version Info
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | awk '{print $2}' | cut -d'+' -f1)
          APP_NAME=$(grep '^appName:' pubspec.yaml | awk '{print $2}')
          if [ -z "$APP_NAME" ]; then
            APP_NAME=$(grep '^name:' pubspec.yaml | awk '{print $2}')
          fi
          FILE_NAME="${APP_NAME}_${VERSION}_${{ matrix.package_suffix }}.apk"
          mkdir -p build/app/outputs/flutter-apk
          mv build/app/outputs/flutter-apk/app-release.apk "build/app/outputs/flutter-apk/${FILE_NAME}"
          # export as step output
          echo "file_name=${FILE_NAME}" >> "$GITHUB_OUTPUT"

      - name: Upload APK as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.generate_version_info.outputs.file_name }}
          path: build/app/outputs/flutter-apk/${{ steps.generate_version_info.outputs.file_name }}
          if-no-files-found: error

      # 步骤 8: 上传已签名的 APK 到 Google Drive
      - name: Upload APK to Google Drive (${{ matrix.package_suffix }})
        run: |
          sudo apt-get update && sudo apt-get install -y jq openssl
          printf "%s" "${{ secrets.GDRIVE_CREDENTIALS_BASE64 }}" | base64 --decode > gdrive_creds.json
          jq -e . gdrive_creds.json > /dev/null
          CLIENT_EMAIL=$(jq -r .client_email gdrive_creds.json)
          PRIVATE_KEY=$(jq -r .private_key gdrive_creds.json)
          HEADER_B64=$(echo -n '{"alg":"RS256","typ":"JWT"}' | base64 | tr '/+' '_-' | tr -d '=')
          IAT=$(date +%s)
          EXP=$(($IAT + 3500))
          SCOPE="https://www.googleapis.com/auth/drive"
          PAYLOAD="{\"iss\":\"$CLIENT_EMAIL\",\"scope\":\"$SCOPE\",\"aud\":\"https://oauth2.googleapis.com/token\",\"exp\":$EXP,\"iat\":$IAT}"
          PAYLOAD_B64=$(echo -n "$PAYLOAD" | base64 | tr '/+' '_-' | tr -d '=')
          SIGNATURE_INPUT="$HEADER_B64.$PAYLOAD_B64"
          SIGNATURE=$(echo -n "$SIGNATURE_INPUT" | openssl dgst -sha256 -sign <(printf "%s" "$PRIVATE_KEY") | base64 | tr '/+' '_-' | tr -d '=')
          JWT="$SIGNATURE_INPUT.$SIGNATURE"
          TOKEN_RESPONSE=$(curl -s -X POST "https://oauth2.googleapis.com/token" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer&assertion=$JWT")
          ACCESS_TOKEN=$(echo "$TOKEN_RESPONSE" | jq -r .access_token)
          if [ -z "$ACCESS_TOKEN" ] || [ "$ACCESS_TOKEN" = "null" ]; then
            echo "ERROR: failed to get access token"
            exit 1
          fi

          # 使用来自 Generate Version Info step 的输出
          FILE_NAME="${{ steps.generate_version_info.outputs.file_name }}"
          FILE_PATH="build/app/outputs/flutter-apk/${FILE_NAME}"
          FOLDER_ID="${{ secrets.GDRIVE_FOLDER_ID }}"

          # 检查文件是否存在
          if [ ! -f "$FILE_PATH" ]; then
            echo "ERROR: APK not found: $FILE_PATH"
            ls -l build/app/outputs/flutter-apk || true
            exit 1
          fi

          # 生成 metadata.json（保证严格的 JSON）
          printf '%s' "{\"name\":\"$FILE_NAME\",\"parents\":[\"$FOLDER_ID\"]}" > metadata.json
          echo "----- metadata.json -----"
          cat metadata.json

          # 上传：把 metadata 作为文件上传，避免引号/转义问题
          if ! UPLOAD_RESPONSE=$(curl --fail-with-body -sS -X POST \
            "https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&supportsAllDrives=true" \
            -H "Authorization: Bearer $ACCESS_TOKEN" \
            -F "metadata=@metadata.json;type=application/json;charset=UTF-8" \
            -F "file=@$FILE_PATH;type=application/vnd.android.package-archive"); then
            echo "ERROR: Google Drive upload request failed"
            exit 1
          fi

          FILE_ID=$(printf '%s' "$UPLOAD_RESPONSE" | jq -er '.id') || {
            echo "ERROR: Google Drive upload response does not contain a file id"
            exit 1
          }
          echo "Uploaded $FILE_NAME to Google Drive (file id: $FILE_ID)"


      # 步骤 9: 上传已签名的 App Bundle 到 Google Drive
      # - name: Upload App Bundle to Google Drive
      #   run: |
      #     # 使用 base64 Secret（将原始 gdrive JSON 本地先做 base64 -w0 保存到 Secret）
      #     printf "%s" "${{ secrets.GDRIVE_CREDENTIALS_BASE64 }}" | base64 --decode > gdrive_creds.json

      #     # 从文件读取凭据
      #     CLIENT_EMAIL=$(jq -r .client_email gdrive_creds.json)
      #     PRIVATE_KEY=$(jq -r .private_key gdrive_creds.json)

      #     HEADER_B64=$(echo -n '{"alg":"RS256","typ":"JWT"}' | base64 | tr '/+' '_-' | tr -d '=')
      #     IAT=$(date +%s)
      #     EXP=$(($IAT + 3500))
      #     SCOPE="https://www.googleapis.com/auth/drive"
      #     PAYLOAD="{\"iss\":\"$CLIENT_EMAIL\",\"scope\":\"$SCOPE\",\"aud\":\"https://oauth2.googleapis.com/token\",\"exp\":$EXP,\"iat\":$IAT}"
      #     PAYLOAD_B64=$(echo -n "$PAYLOAD" | base64 | tr '/+' '_-' | tr -d '=')
      #     SIGNATURE_INPUT="$HEADER_B64.$PAYLOAD_B64"
      #     SIGNATURE=$(echo -n "$SIGNATURE_INPUT" | openssl dgst -sha256 -sign <(echo -n "$PRIVATE_KEY") | base64 | tr '/+' '_-' | tr -d '=')
      #     JWT="$SIGNATURE_INPUT.$SIGNATURE"
      #     TOKEN_RESPONSE=$(curl -s -X POST "https://oauth2.googleapis.com/token" -H "Content-Type: application/x-www-form-urlencoded" -d "grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer&assertion=$JWT")
      #     ACCESS_TOKEN=$(echo "$TOKEN_RESPONSE" | jq -r .access_token)
      #     FILE_PATH="build/app/outputs/bundle/release/app-release.aab"
      #     FILE_NAME="app-release-bundle-${{ github.ref_name }}.aab"
      #     FOLDER_ID="${{ secrets.GDRIVE_FOLDER_ID }}"
      #     METADATA="{\"name\":\"$FILE_NAME\", \"parents\":[\"$FOLDER_ID\"]}"
      #     curl -s -X POST "https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&supportsAllDrives=true" \
      #     -H "Authorization: Bearer $ACCESS_TOKEN" \
      #     -F "metadata=$METADATA;type=application/json;charset=UTF-8" \
      #     -F "file=@$FILE_PATH;type=application/octet-stream"


      # # 步骤 10: 创建 GitHub Release
      # - name: Create GitHub Release
      #   uses: softprops/action-gh-release@v1
      #   with:
      #     generate_release_notes: true
      #     files: |
      #       build/app/outputs/flutter-apk/app-release.apk
      #       build/app/outputs/bundle/release/app-release.aab
